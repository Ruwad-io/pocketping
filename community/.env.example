# Database
DATABASE_URL="postgresql://pocketping:pocketping@localhost:5432/pocketping"

# ─────────────────────────────────────────────────────────────────
# Bridge Configuration (Optional)
# Configure one or more bridges to receive chat messages
# ─────────────────────────────────────────────────────────────────

# Telegram
# 1. Create a bot via @BotFather
# 2. Create a group and make it a supergroup with topics enabled
# 3. Add the bot as admin
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
TELEGRAM_WEBHOOK_SECRET=  # Optional: secure webhook endpoint

# Discord (shared bot for all projects)
# 1. Create an app at https://discord.com/developers/applications
# 2. Create a bot and get the token
# 3. Enable Message Content Intent in Bot settings
# 4. Add bot to your server with permissions: Send Messages, Create Threads, Read Messages
DISCORD_BOT_TOKEN=
# Enable Discord Gateway for receiving messages from operators (requires persistent hosting, NOT serverless)
ENABLE_DISCORD_GATEWAY=true

# Slack
# 1. Create a Slack app with Events API enabled
# 2. Set the request URL to /api/webhooks/slack
# 3. Use the signing secret to verify requests
SLACK_SIGNING_SECRET=

# ─────────────────────────────────────────────────────────────────
# Widget Configuration (Optional)
# ─────────────────────────────────────────────────────────────────

# URL where the widget JS is hosted
NEXT_PUBLIC_WIDGET_URL=https://cdn.pocketping.io/widget.js

# URL of this API server (for widget to connect to)
NEXT_PUBLIC_API_URL=http://localhost:3000
